#include <string.h>

int validate_localport_for_general_yahoo(char * localport){
    // 文字数
    // size_t length = strlen(localport); 
    // printf(length);
    // 構成文字
    // 先頭
 
    // .の連続使用
    return 0;
}